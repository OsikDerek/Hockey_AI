# Video data (too large for git)
data/raw_videos/
data/processed_frames/

# Model weights
models/*.task
models/*.pt
*.pt

# Output (ignore the contents, but keep the directory traversable so the
# reproducible-test fixtures below can be re-included).
output/*
# ...except the hand-annotated ground truth + the scorecard baseline:
# these are test fixtures the scorecard depends on.
!output/_gt/
!output/_scorecard_baseline.json

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Notebooks checkpoints
.ipynb_checkpoints/
models/HockeyAI_model_weight.pt
models/*.pt
