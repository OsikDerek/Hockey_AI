---
name: Subagent editing + direct implementation preference
description: Derek prefers subagents that can edit files, but if a system reminder blocks subagent edits, the main session should implement directly rather than delegating
type: feedback
---

If a session hits a system reminder that blocks sub-agents from editing files, apply the fixes directly in the main session rather than continuing to delegate.

**Why:** In a prior Hockey_AI session, Derek hit this blocker and it stalled progress because the main session kept trying to delegate instead of just doing the edits.

**How to apply:** When the plan is approved and implementation is straightforward file edits across known files, use Edit/Write directly from the main session. Only delegate to subagents when the work is genuinely parallelizable research or when the main context is at risk of bloat from large reads.
